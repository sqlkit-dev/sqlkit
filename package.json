{
  "name": "sqlkit-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter sqlkit build",
    "predev": "bun run build",
    "dev": "mprocs",
    "test": "bun run --filter sqlkit test",
    "format": "bun run --filter sqlkit format",
    "docs": "bun run --filter sqlkit docs"
  },
  "devDependencies": {
    "mprocs": "^0.9.5",
    "typescript": "^5.9.3"
  }
}
